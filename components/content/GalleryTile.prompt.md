One-line: a gallery photo tile — the proof section is built from these.

```jsx
<GalleryTile alt="Instant locs, day one" ratio="3 / 4" caption="Instant Locs" onClick={open} />
```

Omit `src` to get a clearly-labelled swap-me placeholder at the right aspect ratio. Always write real alt text describing the service.
