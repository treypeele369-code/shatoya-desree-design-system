One-line: one row of the pricing menu — never render services as cards.

```jsx
<ServiceRow name="Adult Retwist (Long)" duration="70 min" price="$110" href={BOOKING_URL} />
```

Group rows under a `SectionLabel` category heading. The bottom hairline is built in, so don't add a `Rule` between rows.
