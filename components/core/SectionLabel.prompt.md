One-line: the small-caps eyebrow above every section heading.

```jsx
<SectionLabel index="04">Services & Pricing</SectionLabel>
```

Always pairs with a display-serif heading directly beneath it. Never bold, never sentence case in the source (CSS uppercases it).
