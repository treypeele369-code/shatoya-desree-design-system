One-line: the phone menu — the header's links collapse into this below 900px.

```jsx
<MobileNav open={open} onClose={()=>setOpen(false)} links={NAV} bookingUrl={BOOKING_URL}
  phone="(470) 624-5865" instagram="https://www.instagram.com/shatoya.desree" />
```

Always ends with a full-width primary Book Appointment button.
